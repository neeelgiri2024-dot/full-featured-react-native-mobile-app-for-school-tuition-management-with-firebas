# Example environment variables (do not commit secrets)

# React Native Config (if using react-native-config)
API_BASE_URL=https://api.example.com
FEATURE_X_ENABLED=false

# Firebase config will be injected by CI; do not commit real files
